<h2>Detail outlet</h2>
<ul>
    <li>
      name: {{model.name}}
    </li>
    <li>
      {{#if model.isNew}}
        createdAt: ---
      {{else}}
        createdAt: {{format-time model.createdAt format: 'hhhmmmss'}}
      {{/if}}
    </li>
</ul>
